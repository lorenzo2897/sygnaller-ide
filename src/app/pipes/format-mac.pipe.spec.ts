import { FormatMacPipe } from './format-mac.pipe';

describe('FormatMacPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMacPipe();
    expect(pipe).toBeTruthy();
  });
});
